// For now empty..

/*chrome.runtime.sendMessage(
  {
    message: 'get_name',
  },
  (response) => {
    if (response.message === 'success') {
      $('div').html(`Hello ${response.payload}`);
    }
  }
);*/
